============
Installation
============

At the command line::

    $ pip install abnamrolib

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv abnamrolib
    $ pip install abnamrolib

Or, if you are using pipenv::

    $ pipenv install abnamrolib
